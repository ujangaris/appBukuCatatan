<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/style.css">
    <title>Express Node</title>
</head>

<body>
    <div class="nes-container is-rounded">
        <h1 class="">Express Notes</h1>
        <div class="notes">
            <% if (notes && notes.length > 0){ %>
            <% notes.forEach(function (note){%>
            <div class="note">
                <div class="nes-container is-dark">
                    <h2><a href="#"><%= note.title%>
                        </a></h2>
                    <p><%= note.body%>
                    </p>
                    <span class="nes-text is-disabled date"><%= noteAge(note.created_at) %>
                    </span>
                    <div class=""><a href="#" class="nes-btn is-primary"><i class="nes-icon heart is samll"></i>Edit</a>
                        <a href="#" class="nes-btn is-error"><i class="nes-icon close is-samll"></i>Delete</a>
                    </div>
                </div>
            </div>
            <%  })%>

            <% }else { %>
            <div>No notes yet.. <a href="#" class="nes-btn is-primary">Create on here</a></div>
            <% } %>

        </div>
    </div>
</body>

</html>